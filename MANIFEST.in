# Exclude specific files
# All files which are tracked by git and not explicitly excluded here are included by hatch
# Prune folders
prune build
prune docs/_build
prune docs/api
global-exclude *.pyc *.o
